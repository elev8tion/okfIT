# Don't let pnpm's pre-run deps check abort every script when a build script
# (e.g. esbuild) is in the "ignored" state. The check still runs and warns,
# but `pnpm build` / `pnpm test` / `pnpm typecheck` proceed regardless.
verify-deps-before-run=warn
